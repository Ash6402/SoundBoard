import { createAction } from "@ngrx/store";

export const getAccessToken = createAction("[Home Component] Get Access Token");

export const getTokenSuccess = createAction("[Home Component] Get Token Success");